[project]
name = "playwright-crawl4ai"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "crawl4ai>=0.7.7",
    "ipykernel>=7.1.0",
    "nest-asyncio>=1.6.0",
    "playwright>=1.55.0",
]

[tool.uv.workspace]
members = [
    "Webwright",
]
